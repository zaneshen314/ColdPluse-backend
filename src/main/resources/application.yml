spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coldpulse
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: "root"
    password: "admin"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  flyway:
    baseline-on-migrate: true